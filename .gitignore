node_modules/
dist/
coverage/
*.tsbuildinfo

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local environment - never commit real values
.env
.env.*
!.env.example

# Production-derived data - never commit, pseudonymised or not.
# scripts/p3-equivalence-export.sql produces one of these; it belongs outside
# the tree entirely, and this is only the backstop for when it is not.
*.production-export.json

# Logs
*.log
npm-debug.log*
dist-fixture/
